import 'package:flutter/material.dart';

class WebcamPage extends StatelessWidget {
  const WebcamPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Webcam page"));
  }
}
